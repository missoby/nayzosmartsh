<!DOCTYPE>
<html>
    <head>
        <meta charset="utf-8" />
        <title>Modifier profil</title>
    </head>

    <body>
        <p>
            Profil | <a href="/profil">Profil</a> |
            <a href="/">Home</a>
        </p>
        
        <form action="" method="post">
            <label>
                <strong>Login</strong>
            </label>
            <input type="text" name="nom" value="{{ set_value('nom', getsessionhelper()['nom']) }}" />
            {{ form_error('nom') }}
            <br />
            <label>
                <strong>Email</strong>
            </label>
            <input type="text" name="email" value="{{ set_value('email', getsessionhelper()['email']) }}" />
            {{ form_error('email') }}
            <br />
            <input type="submit" value="Modifier" />
        </form>
    </body>
</html>


